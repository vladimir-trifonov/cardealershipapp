define(["backbone", "jquery", "views/CarTypesView"], function(Backbone, $, CarTypesView) {
    var ModelOfCarView = CarTypesView.extend({
        setSelectedId: function(typeOfCarId) {

        }
    });

    return ModelOfCarView;
});